# Local data-pipeline dependencies only (Week 1-4 scripts under
# ml/data_pipeline/ and ml/evaluation/). GPU training deps (torch, unsloth,
# trl, bitsandbytes) are installed fresh inside each Colab/Kaggle session,
# not here -- see the Implementation Workflow doc, Chapter 3.
pyyaml
datasets
huggingface_hub
distilabel[minhash,openai]
beautifulsoup4
datasketch==1.6.5  # newer datasketch breaks distilabel's MinHashDedup internals
python-dotenv
